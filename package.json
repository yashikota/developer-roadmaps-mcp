{
  "name": "developer-roadmaps-mcp",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "@hono/mcp": "^0.1.0",
    "@modelcontextprotocol/sdk": "^1.12.3",
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}